# 更新日志 · Changelog

本项目以「军师助手」开源名发布（仓库内英文目录 junshi-assistant）。

## [0.5.3] - 2026-08-21

### 修复：主窗口不在她时监控停摆（v0.5.2 兜底失效的根因）
- **根因**：v0.5.2 的兜底用 `ChatInfo()` 读窗口标题，该控件在当前微信版本取不到（返回空）；且 `exists=False` 时直接跳过，`switch_to` 的搜索兜底永远没机会执行
- **锚点换源**：改读聊天输入框控件 Name（`editbox.Name` = 当前会话名，实测可靠；`ChatBox.who` 有缓存会过期，禁用）
- **自愈机制**：目标不在可见列表时每 30s 尝试一次搜索切回（`ChatWith`），成功即恢复处理；其余轮次静默跳过，彻底告别刷屏
- **门控修正**：自愈切回后强制全量读一轮（期间她的消息未读已清零、列表末条未变，空闲门控会把它们永久跳过 → 这正是「就绪后没反应」）；有待重试消息（锚定中断/生成失败）时门控一律放行，重试不再被静默吞掉
- **UI 卡死自愈**：ChatWith 搜索可能残留搜索面板（输入框读不到会话名、消息列表读空，一切读取都不可信）→ 新增 `recover_ui`（Esc + 回聊天页），读空时自动恢复重读；已在目标会话时不再重复点击会话行（实测会扰动窗口）
- **锚定基准修正**：改用「窗口里她最后一条」对比列表末条（原来用「最后一条未处理」，她连发多条且较新的已处理时会永久错位 → 锚定永远不符、3 轮后吞消息）

## [0.5.2] - 2026-08-21

### 修复：会话列表虚拟化导致「未找到目标会话」刷屏
- **根因**：微信会话列表是虚拟化列表，UIA 只能拿到当前渲染的可见行；目标行缺席时 `get_session_info` 返回 exists=False，整条监控链路停摆（日志刷屏 + 就绪后无反应）
- **兜底一**：`get_session_info` 列表探测失败时改读主窗口聊天标题控件（`ChatInfo`），主窗口停在她的会话即以聊天窗口为锚继续工作
- **兜底二**：`switch_to` 列表找不到目标时改用微信搜索定位（`ChatWith`）
- **修复「刷新很多次才识别到回复对象」**：`list_sessions` 合并「可见行 + 主窗口当前会话」，主窗口停在目标会话时下拉框必能选到

## [0.5.1] - 2026-08-21

### 识别提速
- **忙碌即跟**：一轮处理过消息后立即进入下一轮，不再傻等轮询间隔（她连发消息逐条快跟）
- 默认轮询间隔 3s → 2s（设置里可设 1s 最快）

### 回复无上限（风险自担）
- 设置中心新增「回复无上限」开关：关闭冷却 + 每小时上限（0 = 不限）
- 开启时显示醒目风险提示（微信风控/封号/费用），由用户自行决定
- 修复 `max_replies_per_hour=0` 会导致永远不可回复的隐患（现为 0 = 不限）
- 修复设置保存时 0 被 `|| 默认值` 吞掉的清洗 bug

### 只生成模式可视化
- dry 模式生成的回复此前只出现在日志 → 现在进入消息流，虚线「草稿 · 未发送」气泡
- 已发送气泡去掉「（系统推荐第 N 条）」冗余前缀

## [0.5.0] - 2026-08-21

### 生成优化：去AI味 + 真人范例召回
- **去AI味两层**：system prompt 内嵌规则（mode=prompt，默认）或二次重写 pass（mode=pass）
- **真人范例召回**：`data/fewshot/<对象>.json` 按 bigram 词重叠召回最相关的 k 条你本人发过的消息注入 prompt（零依赖）
- 设置中心新增「生成优化」分区：两功能独立开关，召回条数滑杆（1-5），去AI味强度可选

### 项目审计清理（去除冗余）
- 删除陈旧产物：`goutou-auto.spec`（桌面版遗留）、过期计划文档、config.json 中 `ui` 残留键
- `main.py` 死代码清理：`State.is_processed/mark_processed`、`find_target_session`、`extract_her_last_line`、`candidate_messages`
- **set_target 修复**：写 config.json 前重读最新配置再 patch，避免内存旧配置覆盖整份文件
- `docs/architecture.md` 重写为当前 Web 分层架构（引擎链含 fewshot/去AI味 数据流）

### UI 修复
- 「点启动监控报错/两个设置」根因：前端 dist 构建落后于源码，接口错配 → 重建 dist
- 消息气泡修复：宽度塌缩（max-w 移至 flex 子元素）、方向改为 her 左 / me 右（符合微信习惯）
- 设置弹窗底栏固定（不随内容滚动被裁）；侧栏「工具」卡改「回复风格」+ 新增「安全兜底」卡
- 帮助文案同步生成优化说明

## [0.4.2] - 2026-08-21

### 消息识别准确率修复（漏消息根因）
- **根因**：步骤 5 在处理前就把消息指纹标记为"已见"；若锚定中断/切回失败/生成失败，消息被永久吞掉，下轮不会重试
- **修复**：改为"处理成功才标记已见"；生成失败自动重试（连续 3 次才放弃防死循环）
- **锚定中断保护**：连续 3 轮锚定/内容确认不通过才放弃（防误发，也防死循环）
- **启动未读**：`seed(keep_last=1)` 启动时若有未读，最后一条她消息不 seed，第一轮当新消息处理
- **纯表情过滤收紧**：`"表情..."` 前缀仅短文本（≤12字）判定为纯表情，带长文字保留不丢弃
- 新增 4 项回归测试（中断不丢/成功才标记/生成重试/seed keep_last），全部 13 项通过

## [0.4.1] - 2026-08-21

### 风格档案：导入体系
- 新增「粘贴分析」：把自己发过的聊天消息贴进来（每行一条），LLM 直接提炼风格档案，**不依赖 ChatLab**
- 新增「导入档案」：选择/粘贴风格档案 JSON（接受裸档案与导出文件两种格式，字段校验）
- 新增「导出」：当前档案一键下载为 JSON（可分享/备份/跨机迁移）
- 风格弹窗改为三标签页（当前档案/粘贴分析/导入档案）

### 修复：提取风格"没反应"
- 根因：提取链路要串行跑多个 60-90 秒的 ChatLab 子流程，期间弹窗内无任何状态变化，失败仅 3 秒 toast 一闪而过
- 弹窗内联持久显示「分析中 / 失败原因」，完成自动刷新档案
- 提取前预检 ChatLab 可用性（未安装立即报错并引导用粘贴分析/导入）
- FileNotFoundError（chatlab / chatlab.cmd 均不存在）给出明确错误而不是裸异常

## [0.4.0] - 2026-08-21

### Web 版重写（替换桌面 GUI）
- 后端 FastAPI（`web/app.py`）：REST API + WebSocket 实时推送（日志/消息气泡/待确认/状态/异地/风格事件），完全复用 goutou 引擎与 main 轮询逻辑
- 前端 React + Vite + Tailwind CSS v4（`web/frontend/`）：暗色玻璃拟态设计、渐变气泡消息流、待确认卡片（⭐推荐标记/候选挑选）、实时日志、设置中心
- 运行 `python web/app.py` 自动打开浏览器 http://127.0.0.1:8765；CLI 入口 main.py 保留

### 主流 AI 接入
- 设置中心内置服务商预设一键切换：DeepSeek / 阿里百炼（通义千问）/ OpenRouter / 硅基流动 / Kimi / Ollama 本地 / 自定义（任意 OpenAI 兼容）
- 在线拉取模型列表（/models）+ 测试连接；API Key 掩码显示不回传明文
- URL 自动规范化：裸域名 / /v1 / 完整路径三种写法均可

### 修复
- 配置 API 保存时掩码 Key 误覆盖真实 Key（含 * 的回传值自动保留旧值）

### 移除
- 桌面端（ui/gui.py、ttkbootstrap 依赖）整体移除，由 Web 版替代

## [0.3.1] - 2026-08-21

### UI 重写
- 组件库从 CustomTkinter 迁移到 **ttkbootstrap**（MIT、Bootstrap 风格、活跃维护）
- 深色/浅色主题一键切换（🌓），记忆在 config 的 ui.theme，默认 vapor-dark
- 新布局：左侧卡片式控制面板（目标/模式/监控/工具）+ 右侧三标签页（消息流/待确认/日志）
- 消息流气泡深浅两套配色；待确认卡片危险态高亮 + 标签页计数徽标
- 设置中心分组 Labelframe；启动前 API Key 预检引导

### 修复
- **冷却设置无效**：设置里「冷却(秒)写入 `cooldown_seconds`，但实际生效字段是 `min_interval_between_replies_seconds`（已改写正确字段）
- **GUI 下文件日志丢失**：`core.log` 重定向到 UI 后不再写 run.log（现补回，UI 与文件双写）
- **轮询冗余 UIA 爬取**：`process_new_messages` 步骤 7 重复调 `get_session_info`（每次全量爬取，识别变慢根因之一），改为复用已读的 last_line
- **第三方 LLM 端点 404**：用户填 `https://host/v1` 时 URL 拼接错误，新增 `normalize_base_url` 规范化（支持裸域名 / /v1 / 完整路径三种写法）
- **隐私泄露**：`config.example.json` 移除真实姓名/wxid/画像，改为占位符
- 关闭主窗口时 worker 未落盘 state（现 stop 时保存）

### 移除
- customtkinter 依赖、ui/theme.json、ui/theme.py

## [0.3.0] - 2026-08-20

### 新增
- 多候选回复：每次生成 3 条不同风格回复，模型自评最优（⭐推荐）
- 确认模式弹窗选择：用户可从候选里手动挑选一条发送
- 自动模式发送系统推荐条；花钱/见面承诺任何模式都拦截进待确认
- 异地自动分析：配置城市优先，未配置时从 ChatLab 数据规则分析 + LLM 语境分析兜底
- 设置中心：LLM 可插拔（API 地址/Key/模型/温度滑块 + 测试连接）、知识库目录切换、异地城市、监控参数（空值不覆盖已有配置）
- 发送失败自动恢复待确认（可重新发送）；自动模式失败转待确认

### 修复
- 微信 4.1 发送按钮失效：自定义发送流程（粘贴 → Enter → 按钮兜底 → 重试）
- 表情包消息误触发与锚定失败（纯表情过滤 + 内容确认放宽）
- worker 线程崩溃保护（控制队列解包 bug）
- PyInstaller 打包路径（sys._MEIPASS / exe 目录）、platform 包名遮蔽标准库

## [0.2.0] - 2026-08-20

### 新增
- 知识库接入：内置 goutoujunshi 关系心理学知识库（MIT），按信号检索注入
- 架构分层：goutou/ 引擎（signals/kb/approval/prompts/engine）、adapters/ 平台适配层
- 桌面 GUI：单实例、鼠标保护、待确认面板、使用说明、风格档案按对象分离

## [0.1.0] - 2026-08-19

### 新增
- wxauto4 微信自动回复原型：白名单对象、锚定防误发、信号检测、DeepSeek 生成
- 三种模式：dry / 自动发送 / 确认后发送
- 风格档案提取（ChatLab 数据 + LLM 提炼）
